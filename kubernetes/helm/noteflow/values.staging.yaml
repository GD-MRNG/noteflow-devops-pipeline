replicaCount: 1

ingress:
  host: noteflow-staging.example.com
  tls:
    secretName: noteflow-staging-tls

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

hpa:
  minReplicas: 1
  maxReplicas: 2

existingSecret: noteflow-staging-secrets
