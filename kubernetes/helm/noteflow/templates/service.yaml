apiVersion: v1
kind: Service
metadata:
  name: {{ include "noteflow.fullname" . }}
  labels:
    {{- include "noteflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "noteflow.selectorLabels" . | nindent 4 }}
